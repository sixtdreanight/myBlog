# Cloudflare Pages 配置文件
# 此配置将静态站点（./dist 目录）部署到 Cloudflare Pages
name = "blog"
compatibility_date = "2026-03-31"

# 核心配置：指定静态文件所在的目录
[site]
bucket = "./dist"

# 构建配置：指定构建命令
[build]
command = "pnpm run build"
